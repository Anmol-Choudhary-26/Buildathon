export default function ApplicantProfileModal({ request, onClose }) {
  const user = request.seekerId || {};
  const profile = request.seekerProfile || {};
  return <div className="modal-backdrop" role="dialog" aria-modal="true" aria-label="Applicant profile"><section className="modal"><button aria-label="Close applicant profile" onClick={onClose}>×</button><h2>Flatmate profile</h2><p>Contact details unlock only after acceptance.</p><dl><dt>Profession</dt><dd>{user.professionTitle || 'Not specified'}</dd><dt>Work routine</dt><dd>{user.workRoutine || 'Not specified'}</dd><dt>Move-in</dt><dd>{profile.moveInTimeline ? new Date(profile.moveInTimeline).toLocaleDateString() : 'Flexible'}</dd><dt>Budget</dt><dd>{profile.maxBudget ? `₹${profile.maxBudget.toLocaleString()}/month` : 'Not specified'}</dd><dt>Preferred areas</dt><dd>{profile.targetLocations?.join(', ') || 'Not specified'}</dd><dt>Short-term stays</dt><dd>{profile.shortTermAcceptable ? 'Open to short-term' : 'Long-term preferred'}</dd></dl><h3>About</h3><p>{profile.bio || 'No bio shared yet.'}</p><h3>Hobbies</h3><div>{(user.hobbyMatrix || []).map(hobby => <span className="tag" key={hobby}>{hobby}</span>)}</div>{user.spotifyData?.topGenres?.length > 0 && <><h3>Music taste</h3><div>{user.spotifyData.topGenres.map(genre => <span className="tag" key={genre}>{genre}</span>)}</div></>}</section></div>;
}
